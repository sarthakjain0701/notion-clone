'use client';

export default function PageEditorPage() {
  // Rendering is now handled by TabManager in layout.tsx
  // This file exists only to satisfy Next.js routing for /page/[id]
  return null;
}
